In this Python project we used fastAPI ,sqllite and Custom template to do this task.
---
---

# Python version 
    Python 3.11.2

***

## Steps to run the tudo project 

    1) Create Virtual Environment:-
*      First, create a directory name it todoapp and open terminal, and cd into todoapp then you need to type the following command.
>       virtualenv env
*      Now activate the virtual environment
>       source env/bin/activate

***




    2) Installation:-
*      FInstall FastAPI. make sure your environment is activated
>       pip install fastapi
*      Also, install uvicorn to work as the server
>       pip install uvicorn
*      Install package for templating
>       pip install jinja2 python-multipart
*      Install package for database
>       pip install sqlalchemy

***




 ##   3) To run:-
 *     To run the FastAPI app type the following command and hit enter
>       uvicorn main:app --reload

==========================================================================================
# Open the link on the browser (http://127.0.0.1:8000)
